name: vidio-web

on:
  push:
    branches: [main]
    paths:
      - "vidio-web/**"
      - ".github/workflows/vidio-web.yml"
  pull_request:
    paths:
      - "vidio-web/**"
      - ".github/workflows/vidio-web.yml"

concurrency:
  group: vidio-web-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

defaults:
  run:
    working-directory: vidio-web

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: vidio-web/pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Oxlint
        run: pnpm lint

      - name: Oxfmt check
        run: pnpm format:check

      - name: Typecheck
        run: pnpm typecheck

      - name: Unit tests
        run: pnpm test

      - name: Build
        run: pnpm build
        env:
          NEXT_PUBLIC_API_BASE_URL: http://127.0.0.1:18080
          VIDIO_API_BASE_URL: http://127.0.0.1:18080

      - name: Performance budgets
        run: pnpm perf:check

      - name: Install Playwright Chromium
        run: pnpm exec playwright install --with-deps chromium

      - name: E2E tests
        run: pnpm test:e2e
        env:
          CI: true
          MOCK_API_PORT: "18080"
          PLAYWRIGHT_PORT: "3100"
          NEXT_PUBLIC_API_BASE_URL: http://127.0.0.1:18080
          VIDIO_API_BASE_URL: http://127.0.0.1:18080

      - name: Upload Playwright report
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: vidio-web/playwright-report
          retention-days: 7
